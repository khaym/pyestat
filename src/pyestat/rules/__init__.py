"""Rule storage layout (Decision E, Layer 3).

This package holds library-bundled rules under ``builtin/`` and
exposes a loader. Project-local rules live under ``./rules/`` in
the *consumer* project — not here — and user-supplied rules are
passed at call site.
"""
