"""Rule storage layout (Layer 3).

This package holds library-bundled rules under ``builtin/`` and
exposes a loader. Project-local rules live under ``./pyestat_rules/``
in the *consumer* project — not here — and user-supplied rules are
passed at call site.
"""
