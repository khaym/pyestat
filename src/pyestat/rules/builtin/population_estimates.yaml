# Population estimates (statsCode 00200524, 人口推計).
#
# Verified against statsDataId 0003443838 ("年齢（5歳階級）、男女別人口").
# Other tables sharing statsCode 00200524 fall through the
# FingerprintMatcher: they must also have the axes named here for the
# rule to apply.
schema_version: "1"
match:
  statsCode: "00200524"
axes:
  time:
    id: time
    format: monthly_e_stat
  area:
    id: area
value:
  type: number
